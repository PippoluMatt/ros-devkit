# ROS2 Control v2 Backlog

Do not implement these areas in v1.5:

- `hardware_interface::ActuatorInterface`.
- `hardware_interface::SensorInterface`.
- Transmissions.
- Controller YAML.
- Bringup launch files.
- End-to-end controller manager launch workflows.

Add these as explicit v2 work only after the v1.5 SystemInterface package flow is stable.
